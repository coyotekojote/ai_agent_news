## `/verify`と`/code-review`スキルの自動実行停止

Claudeが`/verify`および`/code-review`スキルを自動的に実行しなくなった。これらのスキルを使用する場合は、ユーザーが明示的に`/verify`または`/code-review`コマンドで呼び出す必要がある。

[参考リンク](https://code.claude.com/docs/en/changelog)
